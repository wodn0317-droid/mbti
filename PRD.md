# MBTI 성격 유형 테스트 웹사이트 — PRD

> 문서 버전: v1.0 · 작성일: 2026-07-10 · 상태: 초안(Draft)

---

## 📌 전제 가정(Assumptions)

본 PRD는 다음을 가정하고 작성되었습니다. 사용자 검토 후 수정 가능합니다.

- **(가정)** 타깃 지역/언어: 한국, 한국어 단일 언어 (다국어는 로드맵 후반 확장)
- **(가정)** 로그인 불필요 — 누구나 즉시 테스트 시작 (익명 이용)
- **(가정)** 초기 버전은 **모바일 우선(Mobile-First)** 반응형 웹
- **(가정)** 수익모델: 초기엔 없음(트래픽/브랜딩 목적). 추후 배너 광고 또는 제휴 배치 검토
- **(가정)** 개인정보 수집 최소화 — 통계는 익명 집계값만 저장

---

## 1. 프로젝트 개요

### 1.1 한 줄 정의
> 12개의 짧은 질문에 답하면 나의 16가지 MBTI 유형과 상세 해설을 받고, 결과를 이미지로 저장·공유할 수 있는 성격 유형 테스트 웹사이트.

### 1.2 배경 및 문제 정의
- MBTI는 자기 이해와 대화 소재로 널리 소비되는 대중적 콘텐츠다.
- 기존 정식 검사(예: 공식 MBTI, 16Personalities)는 문항이 많아(40~90문항) 이탈률이 높다.
- **핵심 문제:** "가볍고 빠르게, 그러나 그럴듯한 결과를" 원하는 사용자 니즈를 충족하는 초경량 테스트가 필요하다.

### 1.3 핵심 가치 제안(Value Proposition)
- ⚡ **빠름**: 12문항, 약 1~2분 내 완료
- 🎯 **명확함**: 4개 지표(E/I·S/N·T/F·J/P)를 균형 있게 판별
- 🎨 **공유 매력**: 결과를 예쁜 카드 이미지로 저장·공유 → 자연 확산
- 📊 **재미 요소**: 전체 사용자 유형 분포를 보며 "나는 희귀 유형?" 확인

### 1.4 성공 지표(KPI)
| 지표 | 목표(초기 3개월) |
|------|------------------|
| 테스트 완료율(시작 대비) | ≥ 80% |
| 결과 페이지 → 공유/저장 클릭률 | ≥ 20% |
| 평균 체류 시간 | ≥ 2분 |
| 재방문/공유 유입 비율 | ≥ 15% |

---

## 2. 타깃 사용자 분석

### 2.1 주요 페르소나

**① 김소셜 (22세, 대학생) — 공유러**
- 니즈: 친구들과 결과 비교, SNS 인증
- 페인포인트: 긴 검사는 귀찮음, 결과가 안 예쁘면 공유 안 함
- 사용 맥락: 인스타 스토리/카톡 공유, 지하철·수업 사이 짬 시간

**② 이자기 (29세, 직장인) — 자기이해러**
- 니즈: 내 성향·강점·어울리는 직업 확인
- 페인포인트: 결과 해설이 얕으면 신뢰 안 감
- 사용 맥락: 퇴근 후 모바일, 이직/자기계발 고민 시점

**③ 박호기 (17세, 고등학생) — 재미러**
- 니즈: 순수 재미, 친구들 유형 맞히기 놀이
- 페인포인트: 로그인·가입 요구 시 즉시 이탈
- 사용 맥락: 쉬는 시간, 단체 대화방

### 2.2 사용자 시나리오
- "링크 타고 들어와 → 12문항 후딱 풀고 → 결과 캡처해서 단톡방 공유" (김소셜)
- "내 유형이 ENFP래, 추천 직업이랑 약점 읽어보기" (이자기)

### 2.3 경쟁/대안 대비 차별점
| 구분 | 16Personalities | 본 서비스 |
|------|-----------------|-----------|
| 문항 수 | 60개 | **12개** |
| 소요 시간 | 10분+ | **1~2분** |
| 로그인 | 불필요 | 불필요 |
| 공유 이미지 | 제한적 | **핵심 기능으로 강화** |
| 통계 노출 | 없음 | **유형 분포 제공** |

---

## 3. 핵심 기능 목록

| # | 기능명 | 설명 | 사용자 가치 | 우선순위 |
|---|--------|------|-------------|----------|
| F1 | 인트로/시작 화면 | 서비스 소개 + 테스트 시작 CTA | 진입 장벽 최소화 | P0 |
| F2 | 12문항 테스트 | 축당 3문항, 선택지 응답 수집 | 핵심 경험 | P0 |
| F3 | 프로그레스 바 | 현재 진행률(%) 시각화 | 완주 동기 부여 | P0 |
| F4 | 채점/유형 판정 로직 | 4개 축 점수 → 16유형 산출 | 결과 도출 | P0 |
| F5 | 결과 페이지 | 유형·특징·강점·약점·추천직업 표시 | 핵심 만족 | P0 |
| F6 | 결과 이미지 저장 | 결과 카드를 PNG로 다운로드 | 소장/공유 | P0 |
| F7 | 결과 공유 | 링크/SNS 공유(OG 카드 포함) | 바이럴 확산 | P1 |
| F8 | 유형 분포 통계 | 전체 사용자 유형별 비율 표시 | 재미/비교 | P1 |
| F9 | 이전 질문으로 가기 | 답변 수정 | 사용성 | P1 |
| F10 | 결과 다시하기 | 재테스트 진입 | 재참여 | P2 |

### 3.1 F2 테스트 상세 요구사항
- 총 12문항, **4개 축 × 각 3문항**으로 균등 배분.
- 문항 형식: 두 성향을 대비하는 질문에 대해 **선택지 응답**.
  - 방식 A(권장): 2지선다 (예: "모임에 가면 A) 에너지가 충전된다 / B) 기가 빨린다")
  - 방식 B(가정, 추후): 4~5점 리커트 척도로 확장 가능
- 각 선택지는 한쪽 극(예: E 또는 I)에 **+1점** 부여.
- 한 문항당 하나의 축만 판별(문항–축 매핑 고정).

### 3.2 F4 채점 로직 상세
- 축별로 3문항 → 한쪽 극이 **2점 이상이면 해당 극 확정**(3문항이라 동점 불가 → 동점 처리 불필요).
- 4개 축 결과를 순서대로 조합해 최종 4글자 유형 도출 (예: E+N+F+P → ENFP).
- **엣지 케이스:** 미응답 문항이 있으면 결과 산출 불가 → "모든 문항 응답 필요" 안내(다음 버튼 비활성).
- **(가정)** 리커트로 확장 시엔 축 합산 후 중앙값 기준 판정 + 동점 시 특정 극 우선 규칙 필요.

### 3.3 F6/F7 저장·공유 상세
- 결과 카드 DOM을 이미지로 변환(`html-to-image` 또는 `dom-to-image`) → PNG 다운로드.
- Web Share API 지원 기기: 네이티브 공유 시트 호출.
- 미지원 시: 링크 복사 + 카카오/X(트위터) 공유 버튼 폴백.
- 공유 링크 진입 시 결과 유형이 반영된 **동적 OG 이미지/메타태그** 노출(가정: 유형별 정적 OG 이미지 16종 사전 제작).

### 3.4 F8 통계 상세
- 결과 확정 시 유형 카운트를 서버에 +1 (익명).
- 결과 페이지 하단에 "전체 사용자 중 OO%가 이 유형" + 16유형 분포 미니 차트.
- 어뷰징 방지: **(가정)** 세션/디바이스당 과도한 중복 집계 완화(rate limit 또는 1일 1집계).

---

## 4. 사용자 플로우(User Flow)

### 4.1 메인 플로우
```
[랜딩/인트로]
     │ "테스트 시작" 클릭
     ▼
[질문 1] → [질문 2] → ... → [질문 12]
   │  각 답변 선택 시 자동 다음 이동 + 프로그레스 갱신
   │  (← 이전 버튼으로 수정 가능)
   ▼
[결과 계산 로딩(0.5~1s, 연출)]
     ▼
[결과 페이지]
   ├─ 유형/특징/강점/약점/추천직업
   ├─ [이미지 저장] [공유] [다시하기]
   └─ [전체 유형 분포 통계]
```

### 4.2 예외/분기 경로
- **중간 이탈 후 재진입:** (가정) 진행상태를 localStorage에 임시 저장 → "이어서 하기 / 처음부터" 선택.
- **미응답 상태로 다음 시도:** 다음 버튼 비활성 + 선택 유도 마이크로카피.
- **공유 링크 직접 진입:** 결과 페이지 미리보기 노출 + "나도 테스트하기" CTA로 유입 전환.
- **네트워크 오류(통계 전송 실패):** 결과는 정상 표시, 통계는 조용히 재시도/스킵(사용자 경험 차단 금지).

---

## 5. UI/UX 요구사항 ★

### 5.1 정보 구조(IA) / 사이트맵
```
/                → 인트로(랜딩)
/test            → 테스트 진행(문항 1~12, 단일 페이지 상태 전환)
/result/:type    → 결과 페이지 (type = 16유형 코드, 공유 가능 URL)
/stats           → (선택) 전체 통계 상세 페이지
/about           → (선택) MBTI 소개/면책 고지
```

### 5.2 화면별 상세

**A. 인트로 화면**
- 목적: 흥미 유발 + 즉시 시작
- 요소: 서비스 타이틀, 한 줄 설명("12문항 · 1분이면 끝!"), 대표 일러스트, **[테스트 시작] 대형 버튼**, 부가 정보(소요시간·문항수)
- 인터랙션: 시작 버튼 강조(호버/탭 피드백), 스크롤 없이 첫 화면에서 CTA 보이게

**B. 질문 화면 (테스트 코어)**
- 목적: 부담 없이 빠른 응답
- 레이아웃(모바일 기준):
```
┌───────────────────────────┐
│ ▓▓▓▓▓▓░░░░░░  5/12 (42%)   │ ← 프로그레스 바
├───────────────────────────┤
│                           │
│   Q5. 새로운 사람들과       │
│   함께 있을 때 나는...       │
│                           │
│  ┌─────────────────────┐  │
│  │ A. 에너지가 충전된다   │  │ ← 선택지(탭 시 즉시 다음)
│  └─────────────────────┘  │
│  ┌─────────────────────┐  │
│  │ B. 점점 기운이 빠진다  │  │
│  └─────────────────────┘  │
│                           │
│  [← 이전]                  │
└───────────────────────────┘
```
- 인터랙션: 선택 즉시 부드러운 슬라이드/페이드 전환으로 다음 문항, 프로그레스 애니메이션. 선택된 답은 재방문 시 하이라이트.

**C. 결과 로딩(연출)**
- 목적: 기대감 조성 + 계산 시간 자연스럽게 커버
- 요소: "당신의 유형을 분석 중..." + 로딩 애니메이션(0.5~1s)

**D. 결과 화면 (공유의 핵심)**
- 목적: 만족스러운 결과 + 저장/공유 유도
- 구성:
  1. **결과 카드**(캡처 대상 영역): 유형 코드(예: ENFP) + 유형 별명("활동가") + 대표 컬러/일러스트 + 한 줄 요약
  2. 상세 섹션: 성격 특징 / 💪 강점 / ⚠️ 약점 / 💼 추천 직업 / 🤝 잘 맞는 유형
  3. 액션 바: **[이미지 저장] [공유하기] [다시하기]**
  4. 통계 위젯: "전체의 8.2%가 ENFP" + 16유형 분포 미니 바 차트
- 인터랙션: 결과 카드는 저장 시 워터마크/서비스 URL 포함, 공유 버튼은 네이티브 공유 → 폴백 순.

### 5.3 UX 원칙
- **속도감:** 문항 전환 지연 최소화, 로딩은 연출 목적 외 배제
- **완주 유도:** 프로그레스 바 상시 노출, "거의 다 왔어요!" 마이크로카피(후반부)
- **무가입 원칙:** 어떤 화면에서도 로그인 강요 금지
- **접근성(a11y):** 색상만으로 정보 전달 금지, 버튼 44px+ 터치 타깃, 키보드 포커스/aria-label, 명도 대비 WCAG AA
- **상태 처리:** 빈 상태(통계 데이터 없음 시 대체 문구), 로딩 상태, 에러 상태(공유 실패 토스트) 모두 정의
- **반응형:** 모바일 우선, 태블릿/데스크톱은 카드 중앙 정렬 + 여백 확장

### 5.4 디자인 톤앤매너 (참고안)
- 무드: 밝고 경쾌, MZ 친화적. 유형별 시그니처 컬러(16색 팔레트)로 결과 개성 부여
- 타이포: 가독성 높은 고딕(예: Pretendard), 질문은 큼직하게
- 스타일: 라운드 카드, 부드러운 그림자, 이모지/일러스트로 친근함
- 다크모드: **(가정)** v1.5 이후 지원

---

## 6. 기술 스택

| 레이어 | 제안 | 근거 / 대안 |
|--------|------|-------------|
| 프론트엔드 | **Next.js (React) + TypeScript** | 라우팅·SSR로 결과 페이지 OG 태그/공유 최적화. 대안: Vite+React(정적) |
| 스타일링 | Tailwind CSS | 빠른 반응형 UI 구축 |
| 상태 관리 | React 내장 state / Zustand(경량) | 테스트 진행 상태 관리 |
| 이미지 생성 | **html-to-image** (또는 dom-to-image) | 결과 카드 → PNG. 대안: 서버 사이드 satori/OG 이미지 |
| 공유 | Web Share API + 카카오/X SDK 폴백 | 모바일 네이티브 공유 |
| 백엔드/DB | **Supabase**(Postgres) 또는 Firebase | 통계 카운트 저장·집계. 서버리스로 운영 부담 최소 |
| 호스팅/배포 | Vercel | Next.js 최적, 무료 티어로 시작 |
| 분석 | GA4 / Vercel Analytics | KPI 추적 |

- **(가정)** 문항·유형 해설 데이터는 서버 DB가 아닌 **정적 JSON/파일**로 관리(수정 용이, 조회 빠름). 통계 카운트만 DB 사용.

---

## 7. 데이터 구조

### 7.1 문항 데이터 (정적 JSON)
```json
{
  "id": "q5",
  "axis": "EI",              // EI | SN | TF | JP
  "text": "새로운 사람들과 함께 있을 때 나는...",
  "options": [
    { "label": "에너지가 충전된다", "score": "E" },
    { "label": "점점 기운이 빠진다", "score": "I" }
  ]
}
```
- 총 12개 문항, `axis` 값별로 정확히 3개씩.

### 7.2 유형 해설 데이터 (정적 JSON, 16개)
```json
{
  "code": "ENFP",
  "nickname": "활동가",
  "color": "#F5A623",
  "summary": "열정적이고 창의적인 자유로운 영혼",
  "traits": ["호기심 많음", "공감 능력", "즉흥적"],
  "strengths": ["뛰어난 소통력", "긍정 에너지", "새로운 시도"],
  "weaknesses": ["집중력 유지 어려움", "현실 관리 취약"],
  "jobs": ["기획자", "마케터", "상담사", "크리에이터"],
  "goodMatch": ["INTJ", "INFJ"]
}
```

### 7.3 통계 테이블 (DB)
```
type_stats
├─ type (PK)     : varchar(4)   // 16유형 코드
├─ count         : integer      // 누적 횟수
└─ updated_at    : timestamp
```
- 결과 확정 시 해당 type의 count를 원자적으로 +1 (upsert / RPC).
- 조회 시 전체 합계 대비 비율 계산.

### 7.4 주요 API (개요)
| 메서드 | 경로 | 설명 |
|--------|------|------|
| POST | `/api/stats/increment` | 유형 카운트 +1 (body: {type}) |
| GET | `/api/stats` | 전체 유형 분포 반환 |

- **(가정)** 결과 계산은 전량 클라이언트에서 수행(서버 불필요), 통계 집계만 API 사용.

---

## 8. 개발 우선순위 (로드맵)

### 🚀 Phase 1 — MVP (P0)
- F1 인트로 / F2 12문항 테스트 / F3 프로그레스 바 / F4 채점 로직 / F5 결과 페이지 / F6 이미지 저장
- 문항·16유형 해설 데이터 확정
- **완료 기준:** 시작 → 응답 → 결과 → 이미지 저장까지 끊김 없이 동작

### 📈 Phase 2 — 확산·재미 (P1)
- F7 공유(SNS/링크, 동적 OG) / F8 유형 분포 통계 / F9 이전 질문 이동
- 통계용 DB·API 구축
- **완료 기준:** 공유 유입 측정 가능 + 통계 노출

### 🎨 Phase 3 — 완성도·확장 (P2)
- F10 다시하기 개선, 다크모드, 진행상태 이어하기(localStorage)
- 애니메이션/일러스트 고도화, 리커트 척도 확장 검토
- 다국어(영어) 확장 검토

### ⚠️ 리스크 & 의존성
- 결과 이미지 렌더링의 브라우저/폰트 호환성 → 초기 크로스브라우저 테스트 필수
- 통계 어뷰징(중복 카운트) → rate limit 정책 사전 설계
- MBTI 콘텐츠는 재미 목적 → **"비공식/오락용" 면책 고지** 필요(법적/신뢰 리스크 완화)
- 16유형 해설 텍스트 품질이 만족도 좌우 → 콘텐츠 작성 리소스 확보

---

## ❓ 열린 질문 / 추가 확인 필요

1. 문항 응답 방식: **2지선다** vs **리커트 척도(4~5점)** — 어느 쪽을 채택할까요? (현재 2지선다 가정)
2. 결과 해설 콘텐츠는 직접 작성 vs 기존 자료 참고 — 저작권 이슈 확인 필요
3. 공유 시 결과 미리보기(OG 이미지)를 유형별 정적 16종으로 할지, 동적 생성할지?
4. 통계는 실시간 노출 vs 일정 주기 갱신?
5. 광고/수익모델 도입 시점 및 위치
6. 다크모드·다국어 우선순위 조정 여부
